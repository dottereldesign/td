# Repository workflow

- After completing and verifying requested changes, commit them and push the commit to the `main` branch on `origin`.
- Before pushing, confirm the current branch is `main` and incorporate any upstream changes without force-pushing.
- Do not leave completed work only in the local working tree unless the user explicitly asks not to commit or push it.
